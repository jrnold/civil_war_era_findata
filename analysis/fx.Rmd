---
title: "Exchange Rates"
author: "Jeffrey Arnold"
date: "June 20, 2015"
output: html_document
---

```{r}
source("../R/init.R")
```

There is exchange rate data available from the *Merchants' Magazine* and 
*Bankers' Magazine* for this era.

```{r}
bankers_fx <- read_csv("../data/bankers_magazine_exchange_rates.csv") %>%
  filter(! city == "London" | type == "bankers' bills") %>%
  mutate(rate = 0.5 * (rate_low + rate_high)) %>% 
  select(date, city, rate)
  
```

```{r}
merchants_fx <- read_csv("../data/merchants_magazine_exchange_rates.csv") %>% 
  mutate(rate = 0.5 * (rate_low + rate_high)) %>% 
  select(date, city, rate)
```

```{r}
fxrates <- bind_rows(merchants_fx, bankers_fx) %>%
  mutate(date = as.Date(date))
ggplot(fxrates, aes(x = date, y = rate)) +
  geom_point() + 
  geom_smooth() +
  facet_wrap(~ city, scales = "free_y")
```


